import { SESSION_KEYS } from '../config.js';
import { courseById, getCourses } from '../data/courses.js';
import { getEvents, getRecords } from '../storage.js';
import { escapeHTML, formatClock, formatDate, percent } from '../utils.js';
import { eventLabel, getMetricsRollup } from '../services/events.js';

export function renderAdmin() {
  let authenticated = false;
  try { authenticated = sessionStorage.getItem(SESSION_KEYS.ADMIN) === 'true'; } catch (error) { authenticated = false; }
  if (!authenticated) return `<div class="page"><button class="btn btn-text" data-action="navigate" data-page="more">더보기로</button><section class="admin-login"><p class="eyebrow">로컬 통계 확인</p><h1>관리자 로그인</h1><p class="lead">현재 브라우저의 테스트 통계를 확인하는 데모 화면입니다.</p><div class="admin-warning"><strong>보안 안내</strong><br>실제 운영 인증 기능이 아닙니다.</div><form id="admin-login-form" class="section" novalidate><div class="field"><label for="admin-password">테스트 비밀번호</label><input class="input" id="admin-password" name="password" type="password" inputmode="numeric" required><p class="form-message" id="admin-error" role="alert"></p></div><button class="btn btn-primary btn-block">관리자 화면 열기</button></form></section></div>`;
  const metrics = getMetricsRollup(); const records = getRecords(); const events = getEvents().slice(0,100);
  return `<div class="page"><div class="button-row"><button class="btn btn-text" data-action="navigate" data-page="more">더보기로</button><button class="btn btn-secondary" data-action="admin-logout">관리자 로그아웃</button></div><header class="page-header"><p class="eyebrow">LocalStorage 기반 MVP</p><h1>관리자 대시보드</h1></header><section class="stat-grid"><div class="stat-card"><span>누적 방문</span><strong>${metrics.visits}</strong></div><div class="stat-card"><span>사용자</span><strong>${metrics.uniqueUsers?.length || 0}</strong></div><div class="stat-card"><span>완주</span><strong>${records.length}</strong></div><div class="stat-card"><span>완주율</span><strong>${percent(records.length,Object.values(metrics.courseStarts || {}).reduce((a,b)=>a+b,0))}%</strong></div></section><section class="section"><div class="section-heading"><h2>코스별 이용</h2></div><div class="record-list">${getCourses().map(course => `<div class="record-item"><h3>${escapeHTML(course.name)}</h3><p>조회 ${metrics.courseViews?.[course.id] || 0} · 시작 ${metrics.courseStarts?.[course.id] || 0} · 완주 ${metrics.courseCompletions?.[course.id] || 0}</p></div>`).join('')}</div></section><section class="section"><div class="section-heading"><h2>최근 이용 기록</h2></div><div class="admin-table-wrap"><table><thead><tr><th>시각</th><th>이벤트</th><th>대상</th></tr></thead><tbody>${events.map(event => `<tr><td>${formatDate(event.createdAt)} ${formatClock(event.createdAt)}</td><td>${escapeHTML(eventLabel(event.eventType))}</td><td>${escapeHTML(courseById(event.data?.courseId)?.name || event.data?.restaurantId || '-')}</td></tr>`).join('')}</tbody></table></div></section></div>`;
}
