# 혼디길 프론트엔드 테스트 보고서

## 테스트 환경

- 실행일: 2026-07-16 (Asia/Seoul)
- 운영체제: Windows
- 브라우저: Codex 인앱 브라우저(Chromium 엔진)
- 정적 서버: Python `http.server`
- JavaScript 문법 검사: Node.js v25.2.1 `node --check`
- Leaflet: 1.9.4 CDN

## 테스트한 화면 크기

| 구분 | 뷰포트 | 결과 |
| --- | --- | --- |
| 모바일 | 375 × 812, 390 × 844 | 통과 — 하단 내비게이션, 단일 열, 지도 컨트롤·마커 가로 넘침 없음 |
| 태블릿 | 768 × 1024 | 통과 — 2열 카드/4열 통계, 하단 내비게이션 |
| 데스크톱 | 기본 1265px급 뷰포트 | 통과 — 고정 사이드 내비게이션, 3열 코스 카드 |

모바일 레이아웃의 `clientWidth`와 `scrollWidth`, 태블릿 레이아웃의 그리드 열 수를 함께 확인했습니다.

## 기능별 테스트 결과

| 기능 | 확인 내용 | 결과 |
| --- | --- | --- |
| 온보딩 | 2자 이상 닉네임 입력, LocalStorage 저장, 홈 이동 | 통과 |
| 홈 | 히어로, 추천 코스 3개, 최근 기록 빈 상태 | 통과 |
| 코스 필터 | 새 실제 거리 구간별(2.5km 이하·2.5~3km·3km 초과) 결과 각 2개 | 통과 |
| 코스 상세 | 해시 ID 복원, 메타 정보, 준비물·안전 안내 | 통과 |
| Leaflet 지도 | CARTO Voyager 타일·경로·출발/도착/식당 마커 표시, 새로고침 후 재생성 | 통과 |
| 지도 식당 팝업 | 코스 6km 이내 연결 식당 표시, 메뉴·영업시간·네이버지도·카카오맵 링크 | 통과 |
| 활동 시작 | 진행 코스 저장, 활동 화면 이동, 경과 시간 표시 | 통과 |
| 위치 안내 모달 | 반복 열기/닫기 후 모달 1개만 존재, 포커스 대상 제공 | 통과 |
| 세 단계 인증 | 개발용 테스트 설정에서 출발·사진·도착 완료 상태 전환 | 통과 |
| 완주 | 기록 생성, 최근 기록 ID 저장, 완주 결과 화면 이동 | 통과 |
| 맛집 | 코스 연결 식당, 네이버지도·카카오맵 링크 | 통과 |
| 공유 | Web Share/Clipboard/복사 대체 경로 코드 확인 | 통과(자동 공유 창은 열지 않음) |
| 제주방 | 완주 수·거리 기반 여행자/돌하르방/나무·스티커 성장 | 통과 |
| 기록 | 최근/전체 완주 기록 렌더 | 통과 |
| 화면 설정 | 큰 글씨 클래스 반영, 제주방 배경 `귤밭` 저장·복원 | 통과 |
| 기록 초기화 | 확인 모달 후 진행·완주 관련 키 삭제, 빈 상태 즉시 반영 | 통과 |
| 해시 복원 | `#course/aewol-handam` 직접 새로고침 | 통과 |
| 잘못된 해시 | 404 안내와 홈 버튼 렌더 코드 확인 | 통과 |
| 이전 저장 데이터 | 기존 `hondigil:*` 키 유지, `hondigil_mvp_v3` 마이그레이션 코드 검사 | 통과 |

## 멀티파일·경로 확인

- 6개 프로젝트 CSS 파일 모두 HTTP 200으로 로드
- 30개 이상의 ES Module과 모든 상대 import가 HTTP 200으로 로드
- `index.html`에 인라인 스타일과 대량 화면 마크업 없음
- 앱 로직은 `./assets/js/app.js` 한 개의 module entry로 시작
- 파일명 대소문자와 `.js` 확장자 확인
- 정적 서버 루트와 `/hondigil-frontend/` 하위 경로 양쪽에서 확인
- 로컬 자산, SVG, WebP에 404 없음
- 기본 GitHub Pages 모드에서 존재하지 않는 Django API 요청을 만들지 않도록 API URL을 비활성화

하위 경로 테스트 주소:

```text
http://127.0.0.1:8766/hondigil-frontend/#course/aewol-handam
```

위 주소에서 CSS, JavaScript, 파비콘, 히어로 이미지가 모두 `/hondigil-frontend/` 아래의 상대 경로로 해석됐습니다.

## 코스 지도 거리 회귀 테스트

카드·상세 화면에 표시하는 거리를 지도 폴리라인과 동일한 좌표로 Haversine 계산했습니다. 각 코스의 출발·도착 좌표도 경로의 첫 점·마지막 점과 일치하는지 검사했습니다.

| 코스 | 경로 좌표 수 | 지도 계산 거리 | 화면 표시 | 결과 |
| --- | ---: | ---: | ---: | --- |
| 사려니숲 초보길 | 38 | 2.8km | 2.8km | 일치 |
| 애월 한담 바다산책 | 32 | 1.8km | 1.8km | 일치 |
| 성산 해맞이 러닝 | 36 | 2.7km | 2.7km | 일치 |
| 비자림 숨고르기길 | 42 | 2.5km | 2.5km | 일치 |
| 함덕 바다빛 러닝 | 65 | 3.4km | 3.4km | 일치 |
| 올레 7코스 맛보기 | 67 | 3.6km | 3.6km | 일치 |

390 × 844 뷰포트에서 지도 폭 341.6px 안에 거리 요약(174.0px), 범례(137.3px), 3개 마커가 모두 들어가는 것을 확인했습니다. 실제 로드된 타일 URL도 CARTO Voyager 형식이며 경로 외곽선과 본선 2개 SVG 패스가 생성됐습니다.

## 접근성 확인

- `lang="ko"`, 스킵 링크, `main`, `nav`, `dialog`, 상태 메시지 랜드마크 확인
- 버튼과 아이콘의 접근 가능한 이름 확인
- 필터의 `aria-pressed`, 현재 메뉴의 `aria-current` 확인
- 입력·선택 컨트롤의 label 연결 확인
- 모달의 `role="dialog"`, `aria-modal`, 제목 연결, Escape 닫기, Tab 포커스 순환 구현 확인
- 포커스 표시와 최소 44px 이상의 주요 터치 대상 확인
- 큰 글씨와 `prefers-reduced-motion`/사용자 동작 감소 설정 확인

## 콘솔·네트워크 결과

- JavaScript 콘솔 error/warning: 없음
- 프로젝트 로컬 파일 404: 없음
- 모든 JavaScript 파일 `node --check`: 통과
- Leaflet 지도 재진입/새로고침 오류: 없음
- 모달 반복 실행 중복: 없음
- 활동 화면 렌더 시 기존 경과 타이머 정리 후 1개만 재시작하도록 코드·동작 확인

## 발견한 문제와 수정 내용

1. 단일 HTML에 CSS·데이터·저장소·지도·화면·이벤트가 결합돼 유지보수가 어려웠습니다. 역할별 CSS와 ES Modules로 분리했습니다.
2. GitHub Pages에서 절대 API 경로가 불필요한 404를 만들 수 있어 기본 Django API URL을 빈 값으로 변경했습니다. 동기화 코드는 옵션으로 유지했습니다.
3. 화면 재렌더링 때 지도·경과 타이머가 남을 가능성을 라우터의 공통 정리 단계로 막았습니다.
4. 제공 UI 개선본과 저장소의 LocalStorage 구조가 달라, 이전 단일 키를 현재 키로 변환하는 마이그레이션을 추가했습니다.
5. 기존 네비게이션 경로와 요청된 새 경로가 달라 두 형식을 모두 읽는 별칭 라우팅을 추가했습니다.
6. 공통 아이콘을 CSS 도형 대신 동일 `viewBox`·선 굵기의 SVG 모듈로 통일했습니다.

## 남아 있는 제한사항

- 실제 모바일 Safari와 Android Chrome 기기 대신 Chromium 반응형 뷰포트로 검증했습니다.
- 실제 GPS 권한과 현장 반경 인증은 테스트 환경에서 승인하지 않았습니다. 위치 API의 성공·거부·실패 처리와 개발용 좌표 흐름을 검증했습니다.
- 카메라 촬영과 OS 파일 선택 UI, Web Share 시스템 창은 자동화에서 열지 않았습니다. 파일 형식·10MB 제한·미리보기·메타데이터 저장 로직을 확인했습니다.
- CARTO Voyager/OpenStreetMap 지도 데이터, 외부 폰트, Unsplash 코스 이미지는 인터넷 연결이 필요합니다. 지도 실패 시 텍스트 대체 화면을 제공합니다.
- 코스 경로는 OpenStreetMap 보행로 기반 정적 좌표이며 평면 좌표 거리입니다. 실제 서비스 전 통제·공사·경사·현장 우회로와 식당 영업 정보를 검수해야 합니다.
- Django API 동기화는 GitHub Pages 기본 배포에서 꺼져 있습니다. 같은 출처 백엔드와 함께 배포할 때만 설정해야 합니다.
